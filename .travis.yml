sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t sasioin/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true sasioin/react-test npm test -- --coverage

after_success:
  - docker build -t sasioin/multi-client ./client
  - docker build -t sasioin/multi-nginx ./nginx
  - docker build -t sasioin/multi-server ./server
  - docker build -t sasioin/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push sasioin/multi-client
  - docker push sasioin/multi-nginx
  - docker push sasioin/multi-server
  - docker push sasioin/multi-worker
